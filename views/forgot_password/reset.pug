extends ../layouts/right_card.pug

block card-title
    h1.title 비밀번호 재설정

block card-body
    p 새로 쓸 비밀번호를 입력해주세요.
    if error
        .notification.is-danger= error
    form(method="POST", enctype="application/x-www-form-urlencoded")
        input(type="hidden", name="code", value=code)
        .field
            label.label 비밀번호
            .control
                input.input(type="password", name="password", minlength=5, required, placeholder="비밀번호")
            p.help 비밀번호는 최소 5자리 이상이어야 합니다.
        .field
            label.label 비밀번호 재입력
            .control
                input.input(type="password", name="password_retype", minlength=5, required, placeholder="비밀번호 재입력")
        .field
            .control
                button.button(type="submit") 재설정