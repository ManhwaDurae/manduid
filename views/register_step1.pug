extends ./layouts/card_centered.pug

block card-body
    p.title 만화두레 회원가입
    p.subtitle 회원여부 확인
    if error
        .notification.is-danger= error
    p 동아리 회원만 회원가입할 수 있습니다.#[br]
        | 만약 동아리 회원이 아니시라면 #[a(href="//apply.caumd.club") 여기]서 입부신청하세요.#[br]
        | 회원임에도 오류가 발생한다면 회장단에게 문의해주시기 바랍니다.
    form(action='/register', method="POST", enctype="application/x-www-form-urlencoded")
        if redirect
            input(type="hidden", name="redirect", value=redirect)
        input(type="hidden", name="step", value="1")
        .field
            label.label 이름
            .control
                input.input(type="text", name="name", placeholder="예시: 김만두", required)
        .field
            label.label 학번
            .control
                input.input(type="number", name="studentId", placeholder="예시: 19871234", required)
        .field
            label.label 전화번호
            .control
                input.input(type="text", name="phoneNumber", placeholder="예시: 010-0000-0000", pattern="(\+[0-9]{1,3}-)?[0-9]{2,4}-[0-9]{3,5}-[0-9]{3,5}", required)
        
        input(type="submit", hidden)
        .field.is-grouped.is-grouped-right
            .control
                button.button.goBack 뒤로가기
            .control
                button.button.is-primary(type="submit") 회원확인