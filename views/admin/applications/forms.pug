extends ../../layouts/admin.pug

block container-inner
    h1.title 입부/재입부원서
    h2.subtitle 입부/재입부원서 내역입니다. 승인된 원서는 #[a(href="acceptances") 허가내역]에서 확인할 수 있습니다.
    p 입부/재입부원서 내역에서 승인/불허 버튼을 누르면 사유를 입력하지 않고 바로 처리됩니다. 사유를 입력하고 싶으시다면 "자세히" 버튼을 누르세요.#[br]
      | 승인시 재학 학적/준회원명부로 입력됩니다. 필요하시면 승인 후 수정하세요.
    .table-container
        table.table.is-striped.is-fullwidth
            thead
                tr
                    th 원서
                    th 성명
                    th 학번
                    th 학과
                    th 생일
                    th 전화번호
                    th 승인/자세히
            tbody
                each i in forms
                    tr
                        td= i.reapplication ? "재입부" : "입부"
                        td= i.name
                        td= i.studentId
                        td= i.department
                        td= i.birthday
                        td= i.phoneNumber
                        td
                            a(href=`/admin/applications/accept/${i.applicationId}`)
                                button.button.is-link.is-primary.is-small 승인
                            a(href=`/admin/applications/reject/${i.applicationId}`)
                                button.button.is-link.is-danger.is-small 불허
                            a(href=`/admin/applications/form/${i.applicationId}`)
                                button.button.is-small 자세히