extends ../layouts/admin.pug

block container-inner
  if isPresident
    p 회장입니다. 모든 권한을 이용할 수 있습니다
  else if executiveType
    if root
      p root 권한을 부여받았습니다. 모든 권한을 이용할 수 있습니다.
    else
      p 일부 권한을 부여받았습니다. 부여된 권한은 다음과 같습니다.
      .content
        ul
          each i in executiveType.permissions
            li= i.permission
