extends ./layouts/card_centered.pug
include ./mixins/memberFormFields.pug

block card-body
    p.title 만화두레 입부/재입부신청
    if error
        .notification.is-danger= error
    p 중앙대학교 서울캠퍼스 중앙동아리 만화두레에 오신 것을 환영합니다!#[br]
    | 온라인 입부신청 오류 발생시 회장단에게 연락해주시기 바랍니다.
    form(method="POST", enctype="application/x-www-form-urlencoded")
        if redirect
            input(type="hidden", name="redirect", value=redirect)
        +memberFormFields
        .field
            button.button.is-primary(type="submit") 신청